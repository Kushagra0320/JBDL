package ExceptionHandling;

public class Child extends Parent{
    @Override
    public void m(){
// the child class either don't throw any exception or throw same exception as same as Parent class.
        // if parent don't declare any exception then child can either declare run time exception or dont throw any exception.
    }

}
